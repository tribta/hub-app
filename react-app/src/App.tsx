import { Button, HStack } from "@chakra-ui/react";

const App = () => {
  return (
    <HStack>
      <Button>Click Me</Button>
      <Button>Click Me</Button>
    </HStack>
  );
};

export default App;
